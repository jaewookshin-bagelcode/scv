interface:
  display_name: "Commit"
  short_description: "Create scv repository commits safely"
  default_prompt: "Use $commit to create a conventional commit for the current scv repository changes."

policy:
  allow_implicit_invocation: false
